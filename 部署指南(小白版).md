# idlework Records 上线 & 日常更新 · 小白零基础指南

> 目标：把你这个静态网站放到互联网上（输入 idlework.asia 就能访问），
> 并且以后你只需要在自己电脑上改内容 → 点几下 → 网站就自动更新。
> 你**不需要写任何代码**。本指南假设你完全没碰过 GitHub。

---

## 一、先搞清楚几个“人话”概念（30 秒）

- **GitHub**：一个网盘（但专门存网站/代码）。你要把网站文件放上去，它帮你托管。
- **GitHub 仓库（Repository）**：就是 GitHub 上的一个“文件夹”，里面放着你的网站所有文件。
- **GitHub Pages**：GitHub 自带的一项免费服务，能把你仓库里的网页文件变成真正的网址让人访问。
- **GitHub Desktop**：电脑上装的一个小软件，让你**不用敲命令**就能把本地改动传到 GitHub（点几下鼠标就行）。
- **idlework.asia**：你买的域名。绑定后，别人输这个域名就跳到你的 GitHub Pages 网站。
- **你本地已经有的文件夹**：`record-label-site`（在 `C:\Users\Zhenghao\WorkBuddy\2026-07-31-23-41-04\record-label-site`）。这就是你的网站“源头”。

---

## 二、第一次上线的 5 个步骤（只需做一次）

### 第 1 步：在 GitHub 网站新建一个“仓库”

1. 打开浏览器，登录 https://github.com （你的账号是 `chongchanhowl`）。
2. 右上角点 **“+”** 号 → 选 **“New repository”**（新建仓库）。
3. 填写：
   - **Repository name（仓库名）**：填 `idlework-site`
     （用来和你的个人作品集 `chongchanhowl.github.io` 分开，互不干扰）
   - **Description（描述）**：可填 `idlework Records 官方网站`
   - **Public（公开）**：必须选 **Public**（私有仓库 GitHub Pages 要收费）
   - **⚠️ 下面三个勾选项全部不要勾**：
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
     （因为你的文件夹里已经都有这些了，勾了反而会冲突）
4. 点绿色按钮 **“Create repository”**（创建仓库）。
5. 创建后会看到一个几乎空白的页面，上面有一串网址，类似：
   `https://github.com/chongchanhowl/idlework-site`
   **记一下这个网址**（后面第 2 步要用）。

### 第 2 步：安装 GitHub Desktop 并“发布”你的本地文件夹

1. 下载安装 GitHub Desktop：https://desktop.github.com/ （点 Download，一路下一步安装）。
2. 打开 GitHub Desktop，用你的 GitHub 账号登录。
3. 顶部菜单 **File（文件）** → **Add Local Repository…（添加本地仓库）**。
4. 在弹出的窗口里，点 **Choose…（选择）**，找到并选中你的文件夹：
   `C:\Users\Zhenghao\WorkBuddy\2026-07-31-23-41-04\record-label-site`
   然后点 **Add Repository**。
   （如果它提示“这不是一个 Git 仓库”，点 **Initialize** / 初始化即可。）
5. 这时左边会列出很多改动（你的网站文件）。在左下角：
   - **Summary（摘要）** 框里随便写一句，比如 `首次上传网站`
   - 点蓝色按钮 **Commit to master**（或 **Commit to main**，取决于你的 Git 默认分支名）。
   - 注意：分支名 `master` 和 `main` 功能完全一样，只是名字不同；后面 Pages 设置里选同一个名字即可。
6. 提交后，顶部会出现 **Publish repository**（发布仓库）按钮，点它。
   - 确认仓库名是 `idlework-site`、账号是 `chongchanhowl`、保持 **Public**。
   - 点 **Publish**（发布）。
7. 等进度条走完 —— 你的网站文件就已经上传到 GitHub 了！
   - 如果 GitHub Desktop 顶部显示 **“Push origin”** 而不是 “Last fetched”，说明还没真正推上去，**要点 “Push origin”** 等它转完。

### 第 3 步：开启 GitHub Pages（让网址能被访问）

1. 回到浏览器，打开你的仓库页面 https://github.com/chongchanhowl/idlework-site
2. 点上方 **Settings（设置）** → 左侧 **Pages**。
3. 你会看到一个 **Build and deployment（构建和部署）** 区域：
   - **Source（来源）** 已经默认选中 **Deploy from a branch**（不要改成 GitHub Actions）。
   - 下面会出现两个下拉框：**Branch（分支）** 选 **master**；**folder（目录）** 选 **/ (root)**。
4. 点 **Save（保存）**。
5. 页面会提示 “Your site is published at …”。大约等 **1–2 分钟**，
   先访问 https://chongchanhowl.github.io/idlework-site/ 看能不能打开。
   （能打开 = 成功，先别关这个页面。）

#### 如果 Branch 下拉框里只有 “None”，没有 master/main
这说明 GitHub 仓库还是空的，文件没推上去：
1. 回到 **GitHub Desktop**，看顶部中间是不是显示 **“Push origin”**。
   - 是就点它，等进度条走完。
2. 打开仓库页面 https://github.com/chongchanhowl/idlework-site ，点 **Code** 标签。
   - 如果看到 `index.html`、`content.json`、`images/` 等文件，说明推上去了。
   - 如果看到 “This repository is empty” 或 “Quick setup”，说明没推上去，回到第 1 步。
3. 刷新 Pages 设置页，再点 Branch 下拉框，应该就能选 master/main 了。

### 第 4 步：绑定你的域名 idlework.asia

1. 登录你买域名的网站（域名服务商，比如阿里云/腾讯云/Namecheap 等）。
2. 找到 **idlework.asia** 的 **DNS 解析设置**（也叫“域名解析 / DNS Management”）。
3. 添加 **4 条 A 记录**（主机记录填 `@`，代表根域名），每条对应一个 IP：

   | 主机记录 | 类型 | 记录值（IP 地址） |
   |---------|------|------------------|
   | @ | A | 185.199.108.153 |
   | @ | A | 185.199.109.153 |
   | @ | A | 185.199.110.153 |
   | @ | A | 185.199.111.153 |

   （如果你的服务商不支持同时多条 @ A 记录，至少先加前两条。）
4. 回到 GitHub 仓库 **Settings → Pages**，在 **Custom domain（自定义域名）** 框里填 `idlework.asia`，点 Save。
5. 勾选 **Enforce HTTPS**（强制 https，更安全）。
   - ⚠️ 这一项要等证书签发，**最多可能等 24 小时**才会出现/可勾选，属于正常现象，耐心等。
6. 之后在浏览器输入 **https://idlework.asia** 即可访问你的网站。

### 第 5 步（确认）：以后内容更新的日常流程

这一步是你**每次发新内容**都要做的，写在下面“第三部分”。
第一次上线做到这里就完成了，可以先去喝杯茶，等域名生效。

---

## 三、以后每次更新内容的日常流程（核心！）

你不需要动 GitHub 网站、也不需要写代码，全程本地 + GitHub Desktop 点几下：

> **前提**：你电脑上的 `localhost:8123` 这个本地预览服务要开着
> （平时它常驻运行；如果关了，重新用启动脚本开一下）。

1. **打开后台编辑页**：浏览器访问 **http://localhost:8123/admin.html**
2. **改内容**：在后台增删改你的动态 / 回顾 / 博客 / 艺术家 / 作品 等。
3. **保存草稿**：先在后台点 **「保存更改」**（把改动存进你浏览器，防止丢）。
4. **导出到仓库**：点后台的 **「导出 JSON」** 按钮。
   - **第一次**点会弹窗让你“选择文件夹”——**选中你的 `record-label-site` 文件夹**即可（只需选这一次，以后自动记住）。
   - 它会自动把 `content.json` 写回仓库，并把你新加的图片/视频存成独立文件。
   - 看到提示 “✅ 已直接写回仓库” 就成功了。
5. **打开 GitHub Desktop**：你会看到左边列出了改动（content.json、可能还有新图片）。
6. **写一句说明**：左下角 Summary 框填这次更新了啥，比如 `更新了8月动态`。
7. **提交并推送**：点 **Commit to main** → 再点 **Push origin**（推送）。
8. **等 1–2 分钟**，刷新 https://idlework.asia 就能看到新内容。

就这么简单：**本地改 → 导出 JSON → GitHub Desktop 提交推送 → 完事。**

---

## 四、关于“中国大陆访问速度”的大实话（重要）

你的主要访客来自中国大陆，有两点要心里有数：

1. **GitHub Pages 在中国大陆并不总是稳定快**。有时会比较慢、极少数情况下暂时打不开。
   这是 GitHub 的服务器在境外导致的，不是你网站的问题。
2. **我们已经做的优化（已帮你搞定）**：
   - 把图片从内联 base64 改成了独立文件（可被浏览器缓存、可懒加载）。
   - 一次性把 22 张图从 **43 MB 压到 6.2 MB（省了 85%）**，大图从 3–4 MB 降到 200–500 KB。
   - 前台已开启图片懒加载（lazy load），只加载看到的图。
   这些已经大幅改善了加载速度。

**如果你以后觉得还是不够快**，最彻底的解决办法是把网站也同步到国内节点（例如腾讯云 COS + CDN / 阿里云 OSS + CDN），
我可以帮你配置“GitHub 一推送就自动同步到国内 CDN”的自动化。先上线跑着，
如果大陆朋友反馈慢，再升级也不迟。

---

## 五、常见问题排查

- **后台打不开 / 导出没反应**：确认 `localhost:8123` 服务还开着；用 **Edge 或 Chrome** 浏览器打开 `http://localhost:8123/admin.html`（不要直接双击 admin.html 文件，那样“直接写回仓库”功能用不了）。
- **导出时让我选文件夹，选哪个？**：选 `record-label-site` 这个文件夹本身（不是里面的某个文件）。只选这一次。
- **GitHub Desktop 里看不到改动**：确认你是在 `localhost:8123/admin.html` 点的「导出 JSON」且提示成功；改动只会出现在 content.json / images 里。
- **域名 idlework.asia 打不开**：先确认 https://chongchanhowl.github.io/idlework-site/ 能开（说明网站本身没问题，是域名/DNS 还没生效，通常等几分钟到几小时）；再检查 DNS 的 4 条 A 记录是否填对；Enforce HTTPS 未出现请再等。
- **图片不显示**：一般是内容更新后没重新「导出 JSON」+ 没在 GitHub Desktop 里把 images 一起提交推送。

---

## 六、文件结构速览（你一般不用管）

```
record-label-site/
├── index.html          首页
├── news.html / music.html / article.html …   各页面
├── content.json        ★ 网站所有文字/图片引用（你更新的核心文件）
├── images/             ★ 所有图片（导出时自动管理）
├── videos/             视频（加了才会出现）
├── CNAME               写着 idlework.asia（域名绑定用，别删）
├── .nojekyll           让 GitHub 正确托管（别删）
├── README.md           给懂代码的人看的说明
└── （admin.html / admin.js 等后台工具只在你本机，不会上传到 GitHub）
```
